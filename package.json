{
  "name": "couchsync-website",
  "version": "0.1.1",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "publish": "rm -rf node_modules/.cache && next build && next export && gh-pages -m \"[ci skip] Updates\" -d out -r https://github.com/rkrishn7/couchsync.git"
  },
  "dependencies": {
    "next": "10.0.1",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.6",
    "@types/react": "^16.9.56",
    "typescript": "^4.0.5"
  }
}
